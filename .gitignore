# Python
__pycache__/
*.py[cod]
*$py.class
*.pyo
.Python
venv/
.venv/
backend/venv/

# Env and secrets (do not commit)
.env
backend/.env

# Local SQLite DB (dev data)
*.db
backend/*.db

# Local dev TLS certs (mkcert) — never deploy these
frontend/certs/*.pem
